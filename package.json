{
  "name": "app-menu",
  "version": "1.0.0",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "description": "",
  "scripts": {
    "render-client": "cd frontend && yarn install && yarn build",
    "start-server": "cd backend && yarn install && node server"
  }
}
