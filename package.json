{
  "name": "app-menu",
  "version": "1.0.0",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "description": "",
  "scripts": {
    "render-client": "cd frontend && npm install && npm run build",
    "start-server": "cd backend && npm install && node server"
  }
}
